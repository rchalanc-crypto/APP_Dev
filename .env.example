# .env.example
# Shape reference for environment variables used in this repo.
# Copy to .env (gitignored) and fill in real values.
# Firebase client config belongs in index.html, not here.
# This file is for any server-side or build-time secrets only.

# Example (uncomment and fill in if needed):
# SOME_SECRET_KEY=your_value_here
