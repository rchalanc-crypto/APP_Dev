# GitHub Pages Setup

## Pages source config

TODO: fill in — Settings → Pages → Source branch + folder (/ or /docs); current default is main / root.

## Workflow file

TODO: fill in — .github/workflows/deploy.yml pattern for per-app publishing via actions/deploy-pages.

## Custom domain

TODO: fill in — add CNAME file to repo root, set domain in Pages settings, DNS CNAME record.

## Cache busting

TODO: fill in — Pages CDN caches aggressively; strategies (query string, filename hash, no-cache headers).
