# Tooling Decisions Log

Running log of which AI tool did what on each app.
Format: date | app | decision | tool used | notes

---

2026-05-03 | program-base | initial repo restructure | Claude Code | Restructured single-app repo into multi-app program. Plan drafted in Claude.ai chat. See docs/PROGRAM_PLAN.md.

2026-05-04 | nassims-folly | Phase 1 scaffold | Claude Code | Created apps/nassims-folly/ from static-firebase template with Firebase Email Link auth replacing name-based login. Auth gates on /allowlist/{sha256(email)} in RTDB. Rules in firebase-rules.json. Switched GitHub Pages to Pattern B via .github/workflows/deploy-pages.yml. Spec approved in Claude.ai chat on 2026-05-03.

2026-05-04 | nassims-folly | Phase B deploy | Claude Code | Wired real firebaseConfig values (apiKey, messagingSenderId, appId; storageBucket updated to .firebasestorage.app). Added .gitignore to main. Pushed to main; GitHub Actions workflow ran green. Switched GitHub Pages source to GitHub Actions (Pattern B). nassims-folly live. Ride-tracker continuity verified at root URL.

2026-05-05 | nassims-folly | Phase 2 build | Claude Code | RSVP screen, voyage page skeleton, admin dashboard (RSVP tab + allowlist tab), Firebase rules update (§2.D). Security pass complete. All §2.1–2.4 items from SPEC-PHASE-2.md implemented. Spec approved 2026-05-04.

2026-05-05 | nassims-folly | Phase 2.G — URL cutover | Claude Code + browser | Custom domain follyintenerife.com configured: GitHub Pages custom domain + Cloudflare A records (4×, DNS-only). actionCodeSettings.url updated in index.html. Firebase authorized domains updated (follyintenerife.com added, github.io retained as fallback). End-to-end verified.

2026-05-05 | nassims-folly | Phase 2.H — custom email sender | Claude Code + browser | Firebase Auth custom domain configured for follyintenerife.com. Auth emails now from noreply@follyintenerife.com. SPF/DKIM DNS records at Cloudflare (TXT + 2× CNAME). Blaze plan required (already active). Note: passwordless sign-in email body and display name are locked by Firebase — not customizable via Console.

2026-05-06 | nassims-folly | admin dashboard bug fix | Claude Code | RSVP dashboard was serving stale data (SDK cached /invitees snapshot). Switched to direct REST API fetch (/invitees.json?auth=<idToken>) to bypass SDK local sync tree. Tab switching now reloads data on each click.

2026-05-06 | nassims-folly | Phase 3 planning: SPEC-PHASE-3.md, content seed v2, diary-entries-draft.md | Claude.ai chat | Gnatalee McCringleberry character finalised. keeper_note data model. 50 fun facts, 3 seed diary entries, 20+5 activities. Paragliding + Moustache activities + widgets planned for Phase 4. Activity ranker widget planned. Required Viewing section (Akkersdijk/Block/Pastrana) planned.

2026-05-07 | nassims-folly | Phase 3.5 UI redesign + Expedition Briefs | Claude Code | §A–§E from SPEC-PHASE-3.5.md. Dark token system (asphalt/neon/terracotta palette), Barlow Condensed + Cormorant Garamond + DM Sans. Sticky nav (Tabler Icons, scroll spy, IntersectionObserver). Voyage topbar replaces teal app-header. RSVP darkReveal transition. Hero countdown 4-unit / 1s tick. Expedition Briefs: /content/announcements Firebase node, voyage feed with seenBriefIds localStorage, admin compose tab (4th). Two production bugs fixed: J'Dinklage apostrophe SyntaxError in JS strings; .rsvp-screen display:flex CSS specificity overriding .screen { display:none }.

2026-05-08 | nassims-folly | Phase 4: Activities, Weather Widgets, Required Viewing | Claude Code | §A–§G from SPEC-PHASE-4.md. Required Viewing (3-card YouTube grid, lazy iframes). Activities grid with CATEGORIES filter (9 cats, colour-coded border-left), Firebase onValue onlyOnce. Open-Meteo shared fetch layer with 2-hour localStorage TTL cache. Three wx widgets (Kite/Para/Moust) each with 3 spots, traffic-light score bar, 7-day dot forecast. Activity ranker re-uses El Médano cached data. Property section with confirmed/placeholder gate. seed-phase4.js for 3 air activities. Video ID 5ybJz0oByhA (Akkersdijk) confirmed by Robert.

2026-06-12 | shared + nassims-folly | weather-ensemble shared pattern (SPEC-V2 §0 prerequisite) | Claude Code | Diagnosed SPEC-V2's "widgets returning no data": already fixed by 3e0ce43 (wind_speed_unit=knots → 400; API wants kn) and deployed — spec premise was stale; CORS verified open, all 8 spots' data paths probed green against live API. Built the shared pattern: shared/snippets/weather-ensemble.js (Open-Meteo 4-model ensemble GFS/ECMWF/ICON/GEM, 2h localStorage cache keyed spot+model-set, per-model window medians → cross-model median + spread → confidence bands <5/<10 kn). Folly kite widget upgraded to ensemble + confidence line (trade-wind regime: no thermal logic); all wx cards gained "as of HH:MM" stamps and visible error states. Ride-tracker v2 copies this snippet with its own constants. NOTE: ride-tracker still lives at repo-root index.html (served at follyintenerife.com root) — Session 1 cutover to apps/ride-tracker/ still pending.

2026-06-12 | program-base + ride-tracker | Session 1 housekeeping & guest-viewing cutover | Claude Code | Per docs/SPEC-session1-cutover.md. Ride-tracker moved from repo root to apps/ride-tracker/ (git mv, history preserved); new root index.html is a meta-refresh redirect to apps/nassims-folly/; workflow fallback in deploy-pages.yml repointed to nassims-folly. Firebase rules now live in the repo with CLI deploy config per app: apps/ride-tracker/database.rules.json (sessions + availability, shape-validated, default-deny — reconstructed from app write shapes; diff vs Console at first deploy) + firebase.json/.firebaserc pinning bigwhinybabyteartracker; apps/nassims-folly/ got firebase.json/.firebaserc pinning wal-nassims-folly (rules file already existed; duplicate firebase-rules-v2.json removed — byte-identical). Rules-in-repo policy added to root CLAUDE.md. Glenn's new URL: follyintenerife.com/apps/ride-tracker/.

2026-06-12 | nassims-folly | polish hotfix: admin contrast + El Médano area card | Claude Code | Per SPEC-folly-polish-hotfix.md. Admin light-on-light: .admin-table td → color --charcoal; .admin-textarea explicit white bg + --dark text; #csvPreview div given its already-defined-but-unapplied .csv-preview class; diary published-list cream cards → explicit --dark text. --cream verified still in :root, hover rule untouched. Kite widget: KITE_SPOTS collapsed to one "El Médano area" entry (28.0436, −16.5388) with a `launches` label field rendered via existing .wx-note — generic render path kept, no special-casing; 1 weather + 1 ensemble fetch instead of 3 each. Cabezo/La Tejita fetches deleted.

2026-06-12 | ride-tracker | v2 build: weather ensemble + activity ranker + AM/PM/EVE slots | Claude Code | Per apps/ride-tracker/SPEC-V2.md. Copied shared/snippets/weather-ensemble.js verbatim (divergence in constants only): Squamish Spit ensemble card with 11–17h window highlight, Coquitlam daily card, Pemberton−Vancouver 14:00 thermal gradient, KITE/MTB/EITHER/NEITHER ranker with thermal-upgrade + gradient-failure fallback. Day precip judged per venue (Spit rain kills KITE, trail rain kills MTB). Second opinions: Windy + Windfinder (squamish_spit slug) iframes lazy-injected on first <details> expand; SWS link-out buttons only. Availability: 14d × AM/PM/EVE chips, legacy boolean = all-day, converts on first edit; local-time date keys replace UTC toISOString keys. Rules deployed via CLI (Node 22 via nvm; system Node 18 incompatible with firebase-tools 15) and live-tested via REST: malformed/unknown-key writes rejected. Deviation: .write kept at $user level, not availability root as in the spec block — prevents whole-tree deletes. Logic verified with node harness against live Open-Meteo (33 assertions). 375px/second-browser passes pending (no browser in WSL env).

2026-06-26 | ride-tracker | diary reconcile + edit-gate + silent-drop hotfix | Claude Code | spec authored Claude.ai. Notes cap 1024->8192 (CLI Step 1, committed this pass to close repo-vs-live drift); silent-drop .catch on add+edit + error banner; editor edit-gate (EDITOR_HANDLE J'Dinklage, currentUser===handle && session.user===handle); 9 MTB (1 Squamish 2026-06-24 / 8 Eagle) + 3 kite; history date-desc. SPEC A2 overridden by live data (kite 6/11+6/14+6/16). Pending: Robert to log 9th Eagle ride.

2026-07-09 | nassims-folly | Required Viewing → per-activity tutorials + briefs sign-off fix | Claude Code | Retired standalone #watch section; folded 2 of its 3 videos into the activities they pair with via optional tutorial={youtube_id,title,jdm_caption,keeper_note}, captions carried verbatim (El Médano←Akkersdijk 5ybJz0oByhA; Subida al Boquerón←Ken Block Gymkhana Ten m_KBvP0_8Tc; Pastrana LXzPkkQirnM dropped). seed-tutorials.html: admin-authed, title-match, idempotent update() on the two children only. Card render adds Tutorial ▶ link-out (noopener), diary/keeper styling, contrast-verified on charcoal (neon 11.65 / parchment jdm 11.99 / smoke keeper 5.55). Nav 7→6; scroll-spy auto-drops (querySelectorAll section[id]). Briefs double sign-off: field attribution (compose dropdown) duplicated hand-typed body sign-offs — now rendered once (suppress field line when body ends with a signoff; no bare dash for empty author); no content changed. Activities not status-gated = live on deploy; tutorial writes live when seed run. Headless: module parses + 13 assertions pass; no browser in WSL for visual pass.

2026-07-09 | nassims-folly | readability: diary prose 18px + evergreen Crew section | Claude Code | Per SPEC-folly-readability-crew.md. Reported current state first: prose-col (~680px centered) + 16.5px base + property=slot4/"Casa" nav (7 items) + #watch retired all already live — not rebuilt. Two changes: (1) .diary-body was inheriting 16.5px base (no explicit size) → font-size:18px (Cormorant small x-height). (2) New #crew section after #briefs before #diary (A1), wrapped .diary-entry .prose-col to share the 680px column; scoped #crew CSS reuses diary/blockquote look (crew-name Barlow 700 neon; blockquote Cormorant italic 18px terracotta border; intro/outro/byline body font). Copy pasted verbatim canon (Management "who are these two people" notice) — J'Dinklage ASCII apostrophe ×5, "the Captain" preserved in both quotes + prose, both quotes <blockquote>, byline "— Robert (Human Mule)"; grep-verified zero curly quotes. No nav item (A2) → scroll-spy untouched. Rules/DB/data untouched. Path-scoped commit (index.html only). Headless WSL: no browser — wide + 375px visual pass pending Robert's live signed-in verify.

2026-07-11 | nassims-folly | ranker geo rework + snapshot-collector fix + El Médano embed (audit-harness verification model) | Claude Code + Firebase CLI + Open-Meteo | Per SPEC-folly-ranker-geo-rework Steps 1–3, five path-scoped commits (guards 511d3aa / ranker 45a0529 / live-pass fixes 5844578 / collectChildren 46fc09c / embed e436ceb). Verification model shift (Robert's ruling): mid-build browser gates replaced by a scratch node audit harness (vm-sandboxed extraction of index.html functions; static lint → logic → synthetic failure injection → live-API asserts → HEAD-snapshot repro; 68 checks final), with Robert's live passes gating commits post-deploy; deployed-site verification = SHA-256 of the live URL vs local HEAD. Found by this model: ecmwf_ifs slug valid live; all-null-model payloads scored as flat calm (fixed); corrupt-cache brick in widgets (pre-existing, separately ruled + fixed); DataSnapshot.forEach truthy-cancel truncating EVERY content list to its first record since Phase 3 (SDK 10.7.1 source inspected to confirm semantics; ground truth via authorized firebase database:get; all collectors now share collectChildren). INSTRUMENT NOTE (record for all future agent-driven checks): Robert's browser-agent console re-passes surface only console.warn/error — never console.log/group — an entire "vanished score group" defect was this instrument blindness; agent-driven console verification must state which log levels it can see. Score table now debug-gated (localStorage.folly_rank_debug='1' or ?rankdebug=1) with an ungated breadcrumb naming the switch. Skeptic touch on the embed (app's only iframe, pinned Windfinder origin, lazy on tap, noopener link-outs, no hotlinking): no criticals; ticketed pre-existing tabler icons @latest unpin (index.html:8). Open tickets: .indexOn created_at (diary/announcements) rules+CLI pass; widget GEM→Canary model swap (then El Médano ranker fetch dedupes via cache); tabler pin.
